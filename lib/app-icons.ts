import { Activity, Gauge, Swords, type LucideIcon } from "lucide-react"

import type { AppIcon } from "@/lib/apps"

export const appIconMap: Record<AppIcon, LucideIcon> = {
  activity: Activity,
  swords: Swords,
  gauge: Gauge,
}
